# ========================================
# Configuration API Web Search (Multi-Provider)
# ========================================
#
# Instructions:
# 1. Copiez ce fichier vers .env:
#    cp .env.example .env
#
# 2. Remplissez les clés API des providers que vous souhaitez utiliser
#
# Note: Le système utilise un fallback automatique (config.yaml):
#   1. Brave (priorité 1) - 2000 req/mois gratuit
#   2. Serper (priorité 2) - 2500 req/mois gratuit
#   3. Tavily (priorité 3) - 1000 req/mois gratuit
#   4. DuckDuckGo (priorité 4) - Illimité gratuit (pas de clé requise)
# ========================================

# Brave Search API (recommandé)
# https://api.search.brave.com/app/dashboard
BRAVE_API_KEY=your-brave-api-key-here

# Serper API (Google Search, fallback)
# https://serper.dev/
SERPER_API_KEY=your-serper-api-key-here

# Tavily API (AI-native search, fallback)
# https://tavily.com/
TAVILY_API_KEY=your-tavily-api-key-here

# Note: DuckDuckGo ne nécessite pas de clé API
